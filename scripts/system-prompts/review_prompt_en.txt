You are a senior software engineer. Please review the code changes below according to the project's coding standards.

=== CODING RULES & STANDARDS ===
{coding_rules}

=== YOUR TASK ===
Analyze the code diff and ONLY list actual issues/violations found based on these criteria:
- **Architecture violations** (Clean Architecture, layer dependencies)
- **GetX controller management** (lifecycle, Get.put/Get.find usage)
- **Type safety** (hardcoded assets, translation strings)
- **Error handling** (Either pattern, Failure types)
- **Naming conventions** (snake_case, PascalCase, camelCase)
- **Code quality** (correctness, bugs, readability, performance)

IMPORTANT REQUIREMENTS:
- Respond ENTIRELY IN ENGLISH
- ONLY list items with errors/violations, DO NOT list items without issues
- ONLY review code present in the diff - DO NOT comment on files not shown in the diff
- If no errors found, say: "✅ Code changes comply well with the project's coding standards. No serious issues detected."
- Format: Markdown bullet points with corresponding emojis (🔴 for critical errors, ⚠️ for warnings, 💡 for improvement suggestions)
- Specific: Point out file path and problematic code line
- Actionable: Provide specific fix suggestions with code examples if needed
- PRIORITY: List critical errors (🔴) FIRST, then warnings (⚠️), then suggestions (💡)
- GROUP ERRORS: Group similar errors together for better readability (e.g., all Clean Architecture issues, all GetX issues, etc.)
- IMPORTANT: NEVER say a file "does not exist" if it's not in the diff - only review what you can see

EXAMPLE FORMAT (must group by error type):

## 🔴 Critical Issues

### Clean Architecture Violations
- File `lib/features/auth/domain/user_entity.dart:5` is importing Data layer.
  → **Fix**: Remove `import '../../data/models/user_model.dart'`

### GetX Controller Management Issues
- File `login_screen.dart:23` uses `Get.put()` in child widget.
  → **Fix**: Use `Get.find<LoginController>()` instead of `Get.put()`

## ⚠️ Warnings

### Hardcoded Strings
- `Text('Login')` at `login_screen.dart:45`
  → **Fix**: Use `Text(context.tr('auth.login'))`
- `Text('Password')` at `login_screen.dart:52`
  → **Fix**: Use `Text(context.tr('auth.password'))`

## 💡 Suggestions for Improvement

### Code Quality
- Consider extracting `LoginForm` widget for reusability
  → **Suggestion**: Create file `login_form_widget.dart`

=== CODE DIFF TO REVIEW ===
{code_diff}
